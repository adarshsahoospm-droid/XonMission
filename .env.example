# Root env template. Do not commit real secrets.
# Copy values into each app's .env / .env.local — see:
#   apps/candidate-app/.env.example
#   apps/recruiter-app/.env.example
#
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-anon-key
